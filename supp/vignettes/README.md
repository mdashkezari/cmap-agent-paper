Populate the vignette templates with **real session outputs only**.

Recommended capture workflow:
1) Run the interaction in the CMAP Chat UI.
2) Copy the *exact* user prompt text.
3) Use the UI's "Copy trace" control (tool-trace panel) and save the trace text (or export tool traces from storage).
4) Record the returned artifact identifiers (filenames or persistent URLs) exactly as produced.

Then fill:
- vignette_01_discovery.tex
- vignette_02_climatology.tex
- vignette_03_colocalize.tex

Optionally, structure your notes using vignette_schema.json to keep the provenance/artifact list consistent.
